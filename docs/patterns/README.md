# Agent orchestration patterns

Each pattern is implemented as its own package under `src/agent_patterns/`.

- `react/`: ReAct (reason + act) loop placeholder
- `plan_execute/`: planning + execution loop placeholder
- `supervisor/`: supervisor-worker delegation placeholder
- `router/`: routing-to-specialists placeholder
- `reflection/`: self-critique and revision placeholder
